

import 'package:flutter/material.dart';

class CustomLoadingImageNewestBook extends StatelessWidget {
  const CustomLoadingImageNewestBook({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      width: 100,
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(12),
        color: Colors.grey
      ),
    );
  }
}

